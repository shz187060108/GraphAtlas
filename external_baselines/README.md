# Official external baselines

Every method is disabled until its official repository and immutable commit are verified. A configured method must provide `external_baselines/wrappers/<method>.py`; wrappers adapt the exported GraphAtlas NPZ to official code and parse genuine official output. They must not reimplement a simplified proxy. Published aggregate values never become seed-level rows.
